const assert = require('assert');

describe('api', function() {
  describe('#exampleTest', function() {
    it('should always pass. This is an example', function() {
      assert.ok(true);
    });
  });
});